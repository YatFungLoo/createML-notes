# createML notes

iOS 18.2 -
[link to tutorial](https://developer.apple.com/tutorials/develop-in-swift/welcome-to-machine-learning)
[link to training model](https://developer.apple.com/documentation/createml/creating-an-image-classifier-model)
(accessed on Jan 5th 2026, website will be different on next ios release)

## Notes

Using the `NaturalLangauge` framework and create `NLTagger` using the
`.sentimentScore` tag scheme. `NLTagger` accepts multiple schemes.

Create a function that takes string and uses the tagger, depending on the size
of the string analysing, select the appropriate unit (e.g. `.paragraph`

```swift
// ignore input parameter when their values are not needed for function's logic

options: []) {_, _ in
	return false
}
```
